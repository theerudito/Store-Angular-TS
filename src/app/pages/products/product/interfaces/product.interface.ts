// interface es un tipo de contrato que define la estructura de un objeto

export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  quantity: number;
  stock: number;
}
